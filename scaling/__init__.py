"""
GRAVITY- Scaling Package
========================
Production-grade scaling infrastructure for Project Anchor.

Modules:
    api.py           — FastAPI REST layer (77 endpoints)
    worker_pool.py   — Celery async task queue
    db_migration.py  — SQLite → PostgreSQL migration
    config.py        — Scaling-specific configuration
"""
